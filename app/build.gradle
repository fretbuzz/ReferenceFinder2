apply plugin: 'com.android.application'

android {
    signingConfigs {
        MyKey {
            keyAlias 'MyKey'
            keyPassword 'supdog12'
            storeFile file('C:/Users/Joe/Documents/my-release-key.keystore.jks')
            storePassword 'supdog12'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.2"
    defaultConfig {
        applicationId "wildhacks2015v2.referencefinder"
        minSdkVersion 15
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:23.1.1'
    compile 'com.android.support:design:23.1.1'
    compile files('libs/commons-io-2.4/commons-io-2.4.jar')
}
